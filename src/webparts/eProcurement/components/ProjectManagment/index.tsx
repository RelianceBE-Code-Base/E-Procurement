import * as React from 'react';

interface IProjectManagement{
    sampleArchivedRequests:any
}

const ProjectManagement:React.FC<IProjectManagement> = ({sampleArchivedRequests}) => {
    
    return (
        <div>
            
        </div>
    );
};

export default ProjectManagement;